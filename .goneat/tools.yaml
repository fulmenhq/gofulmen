scopes:
  bootstrap:
    description: "Essential bootstrap and tooling dependencies"
    tools:
      - goneat
tools:
  goneat:
    name: goneat
    description: "Fulmen schema validation and automation CLI"
    kind: go
    detect_command: goneat --version
    install_package: github.com/fulmenhq/goneat@latest
    version_scheme: semver
    minimum_version: "0.3.0"
